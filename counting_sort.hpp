#ifndef COUNTING_SORT_HPP
#define COUNTING_SORT_HPP


namespace gra {
    namespace sortings {
        void dcmps_into_cells( std::vector<int> Vector, int Lim, std::vector<std::vector<int>> Queries );
    }
}

#endif
